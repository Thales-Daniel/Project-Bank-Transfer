version: "3.9"
services:
  client:
    stdin_open: true
    build:
      context: .
      dockerfile: Dockerfile
    container_name: docker-app
    ports:
      - "3000:3000"
    volumes:
      - "home/app/node_modules"
      - "./:/home/app"
